# S02 음성 입력 화면 와이어프레임 재구현 가이드

이 문서는 이미 구현된 모바일 기능을 다시 만드는 문서가 아니다. 현재 실기기에서 동작하는 S02의 녹음·권한·음성 변환·위치·시간 기능은 유지하고, 화면 구성과 시각적 위계를 제공된 와이어프레임에 가깝게 다시 만드는 작업만 다룬다.

이번 작업 범위는 **S02 음성 입력 화면만**이다. S01, S03 이후 화면, S06 및 API 기능은 이 문서의 범위가 아니다.

---

## 1. 현재 문제

현재 S02는 기능은 동작하지만 다음 문제 때문에 서비스의 핵심인 `음성으로 사건을 설명하고 AI가 텍스트로 변환·분석한다`는 인상이 약하다.

- 입력 방식 선택 UI가 화면 상단에서 큰 비중을 차지한다.
- 음성 인식 문장 카드가 마이크보다 먼저 보여 시선이 분산된다.
- 위치·시간 입력 영역이 크고 복잡해 주 기능처럼 보인다.
- 큰 마이크 버튼, 녹음 상태, 경과 시간이 하나의 중심 영역으로 묶이지 않는다.
- 녹음 중지, 다시 녹음, 텍스트 입력 버튼의 우선순위가 와이어프레임과 다르다.
- 작은 기기에서는 핵심 녹음 조작보다 부가 입력과 안내가 먼저 보일 수 있다.

S02의 첫인상은 반드시 다음 순서여야 한다.

```text
큰 마이크 버튼
  → 녹음 중 상태와 시간
    → 실시간 또는 최종 인식 문장
      → 녹음 중지
        → 다시 녹음 / 텍스트 입력
```

위치·시간과 오프라인 안내는 이 흐름을 보조하는 하단 정보여야 한다.

---

## 2. 이번 작업에서 유지할 기능

다음 기능은 이미 구현되어 있으므로 삭제하거나 다시 작성하지 않는다.

- Expo Audio 기반 마이크 권한 확인과 요청
- 녹음 시작·중지·취소·resource 정리
- background 또는 unmount 시 활성 녹음 정리
- 녹음 경과 시간 갱신
- 녹음 결과를 음성 처리 service로 전달
- 인식 문장을 `draft.statement`에 저장
- 마이크 권한 거부 및 녹음 오류 처리
- 텍스트 대체 입력
- 현재 위치 권한, 조회, 직접 입력 fallback
- 현재 시간 표시와 수정
- S03 이동 조건과 draft 유지

UI를 수정하면서 Screen의 비동기 처리 순서와 device service 경계를 바꾸지 않는다. UI에 꼭 필요한 상태가 부족할 때만 `VoiceInputViewProps`를 최소한으로 보완한다.

---

## 3. 목표 화면의 시각적 우선순위

### 3.1 최상단 헤더

- 좌측: 뒤로가기 화살표
- 중앙: `분실·도난 신고`
- 현재 `FlowHeader`를 재사용한다.
- 헤더 아래에 와이어프레임에 없는 큰 탭이나 설명 카드를 추가하지 않는다.

### 3.2 사건 설명

- 제목: `무슨 일이 있었는지 알려주세요`
- 설명: `사건의 경위를 말씀해주시면 자동으로 신고서 초안을 작성합니다.`
- 제목은 왼쪽 정렬하고 두 줄이 되더라도 마이크 영역을 밀어내지 않도록 간격을 작게 유지한다.
- 제목과 설명 다음에는 충분한 여백을 두어 시선이 중앙 마이크로 이동하게 한다.

### 3.3 중앙 마이크 영역

S02의 가장 중요한 영역이다.

- 화면 중앙에 원형 마이크 버튼을 크게 배치한다.
- 바깥 원은 단순한 고정 테두리가 아니라 **녹음 중임을 알려주는 펄스·리플 애니메이션**으로 사용한다.
- 안쪽 원은 primary blue로 채운다.
- 마이크 아이콘은 흰색으로 표시한다.
- 눌림 상태에는 opacity 또는 scale 변화를 제공한다.
- idle, 녹음 중, 처리 중에도 마이크 영역의 위치가 크게 움직이지 않아야 한다.
- 권장 크기:
  - 리플 애니메이션 기준 원: 약 `96~104`
  - 안쪽 원: 약 `84~92`
  - 리플 최대 확장 크기: 약 `128~144`
  - 최소 터치 영역: `48`

#### 녹음 중 리플 효과

- `recordingState === "recording"`일 때만 실행한다.
- 중앙 마이크 버튼의 크기와 위치는 고정하고, 그 뒤에 absolute 위치의 원형 레이어를 둔다.
- 원형 레이어 2~3개가 중앙에서 바깥으로 순차적으로 퍼져나가야 한다.
- 각 원은 커질수록 opacity가 낮아져 자연스럽게 사라진다.
- 권장 동작:
  - scale: `1`에서 약 `1.35~1.5`
  - opacity: 약 `0.3~0.4`에서 `0`
  - 한 주기: 약 `1.6~2초`
  - 각 원의 시작 간격: 약 `500~700ms`
  - 끊김 없이 반복
- 리플 레이어에는 `pointerEvents="none"`을 적용해 마이크 터치를 방해하지 않게 한다.
- 녹음이 정지·취소·실패하거나 음성 처리 상태로 전환되면 애니메이션을 즉시 정리한다.
- 애니메이션 시작·종료 때문에 주변 레이아웃이나 마이크 위치가 움직이면 안 된다.
- React Native 기본 `Animated`의 `scale`과 `opacity`만으로 우선 구현할 수 있다. 이 효과만을 위해 새 animation 패키지를 추가하지 않는다.
- 기기에서 동작 줄이기 설정이 활성화된 경우 반복 애니메이션 대신 연한 고정 halo 하나만 표시한다.

첫 구현은 실제 음량과 무관하게 녹음 상태를 알려주는 일정한 pulse 효과로 만든다. 음성 크기에 따라 리플 크기가 달라지는 진짜 오디오 반응형 효과는 recorder에서 안정적인 음량·metering 값을 받을 수 있을 때 추가 고도화한다. metering 지원이 확정되기 전에는 가짜 음량 반응을 만들지 않는다.

기존처럼 마이크가 여러 카드 사이의 일반 버튼으로 보이면 안 된다. 화면을 처음 봤을 때 가장 먼저 마이크가 보여야 한다.

### 3.4 녹음 상태와 시간

마이크 바로 아래에 중앙 정렬한다.

- 녹음 중: 빨간 상태점 + `녹음 중...`
- 시간: `00:17` 형식, primary blue, 굵은 글씨
- 숫자는 `tabular-nums`를 사용해 시간이 변해도 폭이 흔들리지 않게 한다.
- idle 상태에서는 `눌러서 녹음을 시작하세요`와 `00:00` 또는 짧은 대기 안내를 표시한다.
- 음성 처리 중에는 `음성을 텍스트로 변환하고 있어요`와 spinner를 표시한다.
- 오류 상태에서도 마이크 영역 전체가 사라지지 않아야 한다.

### 3.5 음성 인식 문장 카드

녹음 상태와 시간 아래에 배치한다.

- 연한 파란색 배경
- 얇은 border와 둥근 모서리
- 왼쪽에 primary blue 강조선
- 인식 문장을 따옴표와 함께 표시
- 문장이 길면 작은 화면에서 카드가 무한히 커지지 않도록 적절한 최대 높이와 스크롤 또는 줄 수 제한을 검토한다.
- 문장이 아직 없으면 큰 빈 카드를 노출하지 말고 짧은 placeholder를 표시한다.
- 인식 문장은 마이크보다 시각적으로 강하면 안 된다.

표시 예시:

```text
“신주쿠역에서 가방을 잃어버렸어요.
안에 여권과 카드가 있어요...”
```

### 3.6 주요 동작 버튼

와이어프레임과 같은 우선순위를 사용한다.

1. 전체 너비 primary 버튼
   - 녹음 중: `녹음 중지`
   - 음성 처리 중: loading 표시 및 중복 입력 차단
   - 인식 문장 확인 상태: 현재 S03 이동 정책에 맞는 확인 버튼
2. 그 아래 2열 secondary 영역
   - 좌측: `다시 녹음`
   - 우측: `텍스트 입력`

버튼 요구사항:

- primary 버튼은 화면에서 마이크 다음으로 강한 CTA여야 한다.
- 다시 녹음과 텍스트 입력은 같은 너비로 배치한다.
- 버튼 사이 간격은 약 `6~8`로 유지한다.
- 작은 화면에서도 텍스트가 잘리거나 버튼이 세로로 과도하게 커지지 않아야 한다.
- 현재 화면 상단의 음성·텍스트 tab selector는 제거하거나 이 하단 보조 버튼으로 역할을 통합한다.

### 3.7 위치와 현지 시간

와이어프레임처럼 한 줄의 compact 정보 영역으로 만든다.

```text
[ 현재 위치: 신주쿠구 ] [ 현지 시간: 14:30 ]
```

- 위치와 시간은 각각 화면 너비의 절반 정도를 사용한다.
- 아이콘, 작은 label, 한 줄 값만 기본 상태에서 보여준다.
- 긴 위치는 한 줄 말줄임 처리한다.
- 위치 조회 버튼과 장소·시간 TextInput을 항상 큰 카드로 펼쳐놓지 않는다.
- 사용자가 위치 또는 시간을 누르거나 권한 오류가 발생했을 때만 compact한 수정 UI를 펼친다.
- 위치 권한 거부 시 직접 입력 경로는 계속 제공한다.
- 위치·시간 영역이 마이크와 녹음 CTA보다 먼저 보이거나 더 커지면 안 된다.

### 3.8 오프라인 안내

- 화면 최하단에 작은 아이콘과 한 줄 안내로 표시한다.
- 카드 형태의 큰 안내 박스로 만들지 않는다.
- 색상은 `textSecondary`를 사용한다.
- 예시: `오프라인 환경에서도 음성 인식이 가능합니다.`

실제 음성 처리 방식이 완전한 오프라인을 보장하지 않는다면 구현 시 제품 문구를 `네트워크가 불안정하면 텍스트로 계속 입력할 수 있습니다.`로 교체한다.

---

## 4. 상태별 화면 규칙

### 4.1 녹음 대기

- 큰 중앙 마이크가 활성 상태로 보인다.
- 마이크를 누르면 그 시점에 권한을 요청한다.
- 녹음 중지 버튼은 비활성화하거나 녹음 시작 안내로 대체한다.
- 다시 녹음은 기존 문장이 있을 때만 활성화한다.
- 텍스트 입력은 항상 사용할 수 있다.

### 4.2 권한 요청 중

- 마이크 버튼 위치는 유지한다.
- 마이크 안에 spinner 또는 요청 중 상태를 표시한다.
- 중복 터치를 막는다.
- 별도의 큰 loading 화면으로 전환하지 않는다.

### 4.3 녹음 중

- 와이어프레임의 기본 상태로 취급한다.
- 큰 파란 마이크를 유지한다.
- 빨간 점, `녹음 중...`, 경과 시간을 표시한다.
- primary CTA는 `녹음 중지`다.
- 텍스트 입력 전환 시 활성 녹음을 먼저 안전하게 정리한다.

### 4.4 음성 처리 중

- 마이크 영역과 기존 인식 문장 카드는 유지한다.
- `음성을 텍스트로 변환하고 있어요` 상태를 표시한다.
- 녹음·다시 녹음·텍스트 전환·다음 이동의 중복 실행을 막는다.

### 4.5 인식 완료

- 인식 문장을 카드에 표시한다.
- 다시 녹음과 텍스트 수정 경로를 제공한다.
- 현재 Screen의 S03 이동 정책에 맞춰 확인 CTA를 제공한다.
- S03로 이동하기 전에 `draft.statement.trim()`이 비어 있지 않은지 검사한다.

### 4.6 마이크 권한 거부 또는 녹음 오류

- 큰 마이크 영역을 유지한다.
- 오류 문구는 마이크 또는 관련 버튼 가까이에 표시한다.
- 다시 시도, 기기 설정 열기, 텍스트 입력 중 가능한 동작을 제공한다.
- 오류 카드 때문에 마이크와 텍스트 입력 버튼이 첫 화면 밖으로 밀려나지 않게 한다.

### 4.7 텍스트 입력 모드

- 사용자가 `텍스트 입력`을 선택했을 때만 TextInput을 펼친다.
- 사건 설명 입력창을 중심으로 표시하되, 상단 제목과 compact 위치·시간 정보는 유지한다.
- `음성으로 입력` 버튼을 제공해 큰 마이크 중심 화면으로 돌아갈 수 있게 한다.
- 키보드가 열린 상태에서 입력창과 진행 CTA가 가려지지 않아야 한다.

---

## 5. 파일별 수정 범위

실제 구현을 시작할 때 다음 순서로 작업한다.

### 5.1 `VoiceInputView.tsx`

가장 먼저 수정한다.

- 현재 mode selector 중심 구조 제거
- 제목 → 중앙 마이크 → 상태·시간 → 인식 카드 순서로 재배치
- primary 녹음 CTA와 2열 보조 버튼 구성
- 위치·시간을 compact row로 변경
- 오프라인 안내 축소
- 상태별 조건부 UI 정리
- 와이어프레임과 비슷한 색상, 크기, 간격 적용

View에는 Router, Context, Expo Audio, Expo Location, API를 import하지 않는다.

### 5.2 `VoiceInputView.types.ts`

View 재배치에 꼭 필요한 값만 추가한다.

예상 검토 항목:

- 위치·시간 수정 UI가 열렸는지 여부
- 인식 완료 후 CTA label 또는 상태
- 위치·시간 compact 표시값
- 권한 오류별 보조 동작

기존 props로 표현할 수 있는 상태를 중복 추가하지 않는다.

### 5.3 `VoiceInputScreen.tsx`

View에서 새로 필요해진 이벤트와 상태만 연결한다.

- 기존 녹음, 권한, 처리, cleanup 로직 유지
- 기존 위치와 시간 처리 유지
- 단순 편집 UI open 여부는 Screen 로컬 상태로 관리 가능
- 디자인 수정 때문에 service 또는 Context 책임을 Screen에 복사하지 않는다.

### 5.4 공통 컴포넌트와 토큰

S02 하나를 맞추기 위해 공통 `Button` 또는 `AppScreen`의 전역 모양을 무리하게 바꾸지 않는다. 다른 화면에 영향이 생기는 변경은 먼저 S02 전용 스타일로 검증한다.

필요하면 다음처럼 S02 전용 표시 컴포넌트를 분리할 수 있다.

```text
src/features/case/components/VoiceRecordButton.tsx
src/features/case/components/VoiceTranscriptCard.tsx
src/features/case/components/LocationTimeSummary.tsx
```

작은 컴포넌트 분리는 동일한 JSX가 복잡해질 때만 사용한다.

---

## 6. 구현 순서

1. 현재 S02 실기기 화면을 기준 스크린샷으로 남긴다.
2. 기능 로직을 건드리지 않고 `VoiceInputView.tsx`의 JSX 순서만 와이어프레임 순서로 바꾼다.
3. 중앙 마이크 영역의 높이와 버튼 크기를 맞춘다.
4. 녹음 상태·시간·인식 문장 카드를 하나의 세로 흐름으로 맞춘다.
5. primary 녹음 CTA와 2열 보조 버튼을 맞춘다.
6. 위치·시간을 compact row로 줄인다.
7. 권한 거부 시 펼쳐지는 직접 입력 UI를 연결한다.
8. 텍스트 입력 모드와 키보드 동작을 확인한다.
9. idle, 권한 요청, 녹음 중, 처리 중, 완료, 오류 상태를 각각 확인한다.
10. 작은 Android 또는 iOS 기기에서 와이어프레임과 나란히 비교한다.
11. 타입 검사와 diff 검사를 실행한다.

정적 검사:

```powershell
pnpm.cmd --filter mobile typecheck
git diff --check
```

---

## 7. 와이어프레임 재현 완료 기준

### 시각적 기준

- 화면 진입 시 큰 마이크가 첫 시선의 중심이다.
- 제목 다음에 불필요한 mode tab이나 큰 카드가 먼저 나오지 않는다.
- 마이크, 녹음 상태, 시간, 인식 문장이 하나의 중심 흐름으로 보인다.
- 녹음 중에는 `녹음 중지`가 가장 강한 CTA다.
- 다시 녹음과 텍스트 입력은 동일한 2열 보조 버튼이다.
- 위치와 시간은 한 줄 compact 영역이다.
- 오프라인 안내가 작은 하단 문구로 보인다.
- 작은 화면에서도 마이크와 주요 녹음 CTA가 부가 정보 때문에 밀려나지 않는다.
- 와이어프레임과 실기기 스크린샷을 나란히 봤을 때 정보 순서와 시각적 무게가 유사하다.

### 기능 회귀 방지 기준

- 마이크 권한은 사용자가 녹음을 시작할 때 요청된다.
- 실제 녹음 시작·시간 갱신·중지가 동작한다.
- 음성 처리 결과가 `draft.statement`에 남는다.
- 다시 녹음이 기존 문장을 의도대로 초기화한다.
- 마이크 거부 후 텍스트 입력이 가능하다.
- 위치 거부 후 장소 직접 입력이 가능하다.
- background 또는 화면 이탈 시 녹음 resource가 정리된다.
- 인식 문장이 없으면 S03로 이동하지 않는다.
- Screen과 View 책임 분리가 유지된다.

---

## 8. 이번 문서 작성 후 바로 할 일

아직 코드는 수정하지 않는다.

다음 구현 작업을 시작할 때는 S02만 대상으로 아래 두 파일을 먼저 열어 비교한다.

```text
apps/mobile/src/features/case/views/VoiceInputView.tsx
apps/mobile/src/features/case/views/VoiceInputView.types.ts
```

첫 구현 목표는 기능 추가가 아니라 **와이어프레임과 같은 큰 중앙 마이크 중심 화면으로 시각적 구조를 바로잡는 것**이다.

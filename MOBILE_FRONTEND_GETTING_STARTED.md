# 모바일 프론트엔드 작업 시작 가이드

이 문서는 이 저장소에서 모바일 프론트엔드를 처음 맡은 개발자가 **무엇을 먼저 확인하고, 어떤 순서로 구현하며, 다른 담당자와 무엇을 합의해야 하는지** 안내합니다. 프로젝트 전체 범위와 정책은 루트의 [`README.md`](README.md)를 기준으로 하며, 이 문서는 그중 React Native·Expo 담당자의 실행 순서를 구체화합니다.

## 0. 지금 저장소의 상태부터 이해하기

현재 모바일 앱은 완성된 기능을 붙이는 단계가 아니라 **초기 스캐폴드 단계**입니다.

- 앱 위치: `apps/mobile`
- 기술: React Native, Expo, Expo Router, TypeScript
- 현재 라우트: `app/index.tsx` 한 개
- 현재 공통 레이아웃: `app/_layout.tsx`
- 현재 화면: 서비스 소개 문구가 있는 홈 화면
- 공통 계약: `packages/shared/src/case.ts`에 사건 유형과 최소 생성 입력 스키마만 존재
- 실제 API: `GET /api/health`만 존재
- 아직 없는 것: 사건 생성·조회 API, 디자인 시스템, API 클라이언트, 상태 관리, 권한 처리, 음성·카메라·위치·지도·Agora 연동
- 패키지 잠금 파일도 아직 없으므로 최초 의존성 설치 시 팀과 함께 생성·공유 여부를 확인해야 함

따라서 첫 작업의 목표는 모든 화면을 한꺼번에 만드는 것이 아니라 다음 세 가지입니다.

1. 누구나 모바일 앱을 실행할 수 있는 개발 기반을 만든다.
2. 백엔드가 완성되기 전에도 mock 데이터로 핵심 흐름을 이동할 수 있게 만든다.
3. 실제 API가 나오면 화면을 뜯어고치지 않고 데이터 계층만 교체할 수 있게 경계를 만든다.

## 1. 작업 시작 전 30분 체크리스트

### 1.1 브랜치와 변경 상태 확인

이 문서를 작성한 브랜치는 `feat/front.docs`입니다. 실제 기능 작업은 팀의 브랜치 규칙에 따라 최신 `develop`에서 별도의 기능 브랜치를 만드는 것을 권장합니다.

```powershell
git branch --show-current
git status --short
git fetch origin
git switch develop
git pull --ff-only origin develop
git switch -c feature/<issue-number>-mobile-foundation
```

아직 커밋하지 않은 팀원의 변경이 보이면 임의로 되돌리지 않습니다. `main`과 `develop`에는 직접 push하지 않고 PR을 사용합니다.

### 1.2 필수 도구 확인

```powershell
git --version
node --version
corepack --version
docker --version
```

루트 `package.json`은 `pnpm@10.14.0`을 지정합니다. `pnpm` 명령이 없다면 프로젝트 루트에서 다음을 실행합니다.

```powershell
corepack enable
corepack prepare pnpm@10.14.0 --activate
pnpm --version
```

Corepack 활성화가 권한 문제로 실패하면 임의로 다른 pnpm 버전을 설치하기보다 팀에 개발 환경을 공유하고 해결합니다. Node 버전은 팀이 정한 버전을 사용하고, 버전이 아직 정해지지 않았다면 Issue에서 확정한 뒤 `.nvmrc` 또는 `package.json`의 `engines`로 고정하는 작업을 제안합니다.

모바일 테스트 방법도 미리 준비합니다.

- Android 실기기: Expo Go 설치, 개발 PC와 같은 네트워크 사용
- Android 에뮬레이터: Android Studio와 가상 기기 준비
- iOS 실기기/시뮬레이터: macOS와 Xcode 필요
- 브라우저 실행은 빠른 UI 확인용이며, 카메라·마이크·위치·Agora의 실제 동작 검증을 대신하지 못함

### 1.3 문서 읽는 순서

다음 순서로 읽으면 의존 관계를 가장 빨리 파악할 수 있습니다.

1. `README.md`: 전체 기능, 기술, 3주 일정, 보안 정책
2. `docs/USER_FLOW.md`: 화면 흐름과 예외 상태
3. `docs/API.md`: 모바일이 호출할 API 계약
4. `packages/shared/src`: 웹·모바일·서버가 함께 사용하는 Zod 스키마와 타입
5. `apps/mobile/app`: 현재 라우트와 화면
6. `apps/mobile/package.json`, `app.json`, `tsconfig.json`: 실행 명령과 Expo 설정

현재 `API.md`와 `USER_FLOW.md`는 골격만 있으므로, 문서에 없는 요청·응답 형식을 화면 코드에서 임의로 확정하면 안 됩니다.

## 2. 최초 로컬 실행

### 2.1 의존성 설치

프로젝트 루트에서 실행합니다.

```powershell
pnpm install
```

설치 후 `pnpm-lock.yaml`이 새로 생기면 실수로 지우지 말고 팀의 잠금 파일 커밋 정책을 확인합니다. 일반적으로 애플리케이션 저장소에서는 같은 의존성 버전을 재현하기 위해 잠금 파일을 커밋합니다.

### 2.2 환경 변수 파일 준비

```powershell
Copy-Item .env.example .env
```

실제 키는 팀의 안전한 채널로 전달받습니다. `.env`는 커밋하지 않습니다. 모바일에 필요한 공개 설정은 서버 비밀값과 분리해야 합니다.

- `OPENAI_API_KEY`, `AGORA_APP_CERTIFICATE`, AWS 비밀키는 모바일 앱에 절대 넣지 않음
- Agora 토큰과 S3 presigned URL은 서버 API에서 발급받음
- Expo 앱에서 접근해야 하는 값은 공개 가능 여부를 확인하고 `EXPO_PUBLIC_` 접두사 사용을 팀과 합의
- 실기기에서 `localhost`는 개발 PC가 아니라 휴대전화 자신을 가리키므로 API 주소에 PC의 LAN IP 또는 배포 개발 서버 주소 사용

예를 들어 모바일 전용 API 주소를 도입한다면 다음과 같이 이름부터 합의합니다.

```dotenv
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.10:3000
```

이 변수는 현재 `.env.example`에 없으므로 코드에서 먼저 사용하지 말고, API 담당자와 이름을 합의한 뒤 `.env.example`도 같은 PR에서 수정합니다.

### 2.3 앱 실행

```powershell
pnpm --filter mobile dev
```

또는 목적에 따라 다음 스크립트를 사용합니다.

```powershell
pnpm --filter mobile android
pnpm --filter mobile ios
pnpm --filter mobile web
```

실행 후 확인할 것:

- Metro 번들러가 오류 없이 시작되는가?
- 홈에 `Travel Guard`가 표시되는가?
- 파일을 수정하면 Fast Refresh가 동작하는가?
- 터미널과 기기 화면에 빨간 오류가 없는가?

### 2.4 API 연결 확인

백엔드를 함께 실행할 때는 별도 터미널에서 다음을 사용합니다.

```powershell
pnpm --filter web dev
```

현재 확인 가능한 API는 `GET /api/health`뿐입니다. 브라우저나 REST 클라이언트에서 다음 응답을 확인합니다.

```json
{ "status": "ok" }
```

실기기에서 연결이 안 되면 코드부터 수정하지 말고 다음을 순서대로 확인합니다.

1. 휴대전화와 PC가 같은 네트워크인가?
2. API 주소가 `localhost`로 되어 있지 않은가?
3. Windows 방화벽이 개발 포트를 막고 있지 않은가?
4. HTTP 개발 주소 사용 시 플랫폼 보안 설정에 막히지 않는가?
5. 서버 터미널에 요청이 도착하는가?

## 3. 코딩 전에 팀과 먼저 확정할 것

다음 항목은 하루 안에 백엔드·웹·인프라 담당자와 합의합니다. 결정되지 않은 항목은 Issue에 질문과 임시 가정을 남깁니다.

### 3.1 화면과 라우트 목록

권장 초안은 다음과 같습니다. 실제 이름은 `USER_FLOW.md`를 보완하면서 확정합니다.

| 순서 | 권장 라우트 | 책임 |
|---|---|---|
| 1 | `/` | 새 사건 시작, 기존 사건 조회 진입 |
| 2 | `/case/new` | 음성·텍스트 상황 입력 |
| 3 | `/case/questions` | AI 추가 질문과 빈 항목 입력 |
| 4 | `/case/review` | 사건 카드 최종 확인, 비밀번호 설정 |
| 5 | `/case/[caseNumber]` | 사건 요약과 진행 상태 |
| 6 | `/case/[caseNumber]/guide` | 해결 가이드, 순서 변경, 완료 처리 |
| 7 | `/case/[caseNumber]/places` | 주변 기관과 지도·경로 |
| 8 | `/case/[caseNumber]/translation` | 현장 양방향 통역 |
| 9 | `/case/[caseNumber]/report` | 신고서 초안·촬영·업로드·분석 결과 |
| 10 | `/case/[caseNumber]/insurance` | 보험 정보, RAG 안내, 내용 비교 |
| 11 | `/case/lookup` | 사건번호·비밀번호 재조회 |

한 화면에 모든 기능을 넣지 말고 사건번호가 필요한 화면은 동적 라우트 아래에 둡니다. 사건번호와 비밀번호를 URL, 로그, 분석 이벤트에 함께 남기지 않습니다.

### 3.2 API 계약

각 화면마다 최소한 아래를 `docs/API.md`와 `packages/shared`에 먼저 확정합니다.

- HTTP method와 path
- request body, path/query parameter
- 성공 응답과 상태 코드
- 검증 실패, 인증 실패, 요청 제한, 서버 실패 응답
- 긴 작업의 상태 값과 polling 또는 재조회 방식
- 파일 업로드 순서와 presigned URL 만료 처리
- 재시도해도 안전한 요청인지 여부
- 날짜·시간의 형식과 timezone 기준

모바일이 독자적인 TypeScript 타입을 복사해 만들지 말고 `@project/shared`의 Zod 스키마에서 타입을 추론합니다. 다만 서버 응답을 `as SomeType`으로 강제하지 말고 앱 경계에서 Zod로 실제 응답을 검증합니다.

### 3.3 디자인과 UX 상태

각 화면은 정상 화면만 그리지 말고 아래 상태를 같이 정의합니다.

- 최초 로딩
- 데이터 없음
- 제출 중과 중복 탭 방지
- 성공
- 검증 오류
- 네트워크 단절과 재시도
- 서버·AI·외부 서비스 오류
- 마이크·카메라·위치 권한의 미요청, 허용, 거부, 영구 거부
- 업로드 또는 긴 작업 진행 중 앱이 background로 이동한 경우

색상, 간격, 글자 크기, 버튼, 입력, 카드, 오류 안내를 화면마다 새로 만들지 않도록 최소 디자인 토큰과 공통 컴포넌트를 먼저 합의합니다.

## 4. 실제 구현 순서

### 단계 1. 모바일 폴더 구조와 공통 기반 만들기

첫 PR은 기능을 많이 넣기보다 다음 경계를 만드는 데 집중합니다.

```text
apps/mobile/
├── app/                  # Expo Router route 파일
├── src/
│   ├── components/       # 공통 UI
│   ├── features/         # case, guide, places, translation 등 기능별 코드
│   ├── services/         # API client, 외부 연동 경계
│   ├── hooks/            # 공통 hook
│   ├── theme/            # 색상, 간격, typography 토큰
│   ├── utils/            # 순수 유틸리티
│   └── mocks/            # 계약과 동일한 mock fixture
└── app.json
```

원칙:

- `app/` 파일은 라우팅과 화면 조합만 담당
- 실제 기능 로직은 `src/features/<feature>`에 배치
- `fetch` 호출을 화면 컴포넌트 안에 직접 흩뿌리지 않음
- API base URL, timeout, JSON parsing, 오류 변환은 `src/services`에서 한 번 처리
- 서버 상태와 입력 중인 폼 상태를 구분
- 새 라이브러리는 필요성과 대안을 Issue 또는 PR에 기록하고 팀 합의 후 추가

이 단계의 완료 조건:

- 홈에서 임시 사건 시작 화면과 조회 화면으로 이동 가능
- 공통 버튼·입력·로딩·오류 UI가 최소 1개씩 있음
- `pnpm --filter mobile typecheck` 통과
- 다른 팀원이 문서만 보고 앱을 실행 가능

### 단계 2. mock으로 핵심 사건 흐름을 세로로 완성하기

백엔드 API를 기다리며 모든 화면을 따로 만들지 않습니다. 다음 한 줄 흐름을 먼저 끝까지 연결합니다.

```text
홈 → 상황 입력 → 추가 질문 → 사건 카드 확인 → 비밀번호 설정 → 사건번호 표시 → 사건 재조회
```

mock 데이터도 임의 객체가 아니라 확정된 Zod 스키마를 통과해야 합니다. API가 아직 미정인 필드는 화면 내부 타입으로 굳히지 말고 `TODO`와 연결 Issue를 남깁니다.

이 단계에서 반드시 처리할 것:

- `LOST`와 `STOLEN`에 따른 조건부 필드
- 필수 입력과 사용자 수정
- 뒤로 가기 시 입력 보존 정책
- 제출 버튼 연속 탭 방지
- 비밀번호 원문을 디바이스 영구 저장소에 저장하지 않기
- 사건번호 복사 기능에서 비밀번호가 함께 복사되지 않게 하기
- 가짜 개인정보만 사용하기

완료 조건은 “화면이 모두 예쁨”이 아니라 한 시나리오를 처음부터 끝까지 끊김 없이 시연할 수 있는 것입니다.

### 단계 3. 실제 API 클라이언트 연결하기

API 계약과 구현이 준비되면 mock adapter를 실제 adapter로 교체합니다.

권장 요청 흐름:

```text
화면 → feature hook/use-case → API client → HTTPS API
                              ↓
                    Zod 응답 검증·오류 변환
```

구현 시 체크:

- base URL 뒤의 `/` 중복을 안전하게 처리
- 네트워크 오류와 HTTP 오류를 구분
- JSON이 아니거나 스키마가 다른 응답을 사용자용 오류로 변환
- 서버의 내부 오류 메시지나 stack trace를 화면에 그대로 표시하지 않음
- 인증 실패 응답으로 사건 존재 여부를 추측할 수 없게 서버 메시지를 그대로 따름
- 요청 중 버튼 비활성화 또는 idempotency 정책 적용
- 개발 로그에 사건 비밀번호, 여권번호, 숙소 상세주소, 문서 내용 출력 금지

첫 실제 연동은 `/api/health`로 연결 환경을 검증하고, 그다음 사건 생성·조회 세로 흐름을 붙입니다.

### 단계 4. 기기 권한을 기능별로 추가하기

권한은 앱 시작 시 전부 요청하지 않고 사용자가 해당 기능을 누를 때 이유를 먼저 설명한 뒤 요청합니다.

| 권한 | 사용 시점 | 거부 시 대안 |
|---|---|---|
| 마이크 | 상황 음성 입력, 통역 시작 | 텍스트 직접 입력 |
| 위치 | 주변 기관 검색 | 지역·주소 직접 입력 |
| 카메라 | 신고서 촬영 | 갤러리 또는 파일 선택 |
| 사진/파일 | 기존 신고서·보험 문서 업로드 | 촬영 또는 나중에 하기 |

패키지를 설치할 때는 Expo SDK와 호환되는 명령을 사용합니다.

```powershell
pnpm --filter mobile exec expo install <package-name>
```

설치 후 `app.json` plugin과 Android/iOS 권한 설명 문구가 필요한지 공식 Expo 문서 기준으로 확인합니다. Expo Go에서 지원되지 않는 네이티브 기능은 development build가 필요할 수 있으므로 Agora 도입 전에 인프라 담당자와 빌드 방식을 먼저 확정합니다.

### 단계 5. 1주차 핵심 기능을 실제로 완성하기

우선순위는 다음과 같습니다.

1. 사건 시작 진입 화면
2. 음성 녹음 시작·중지와 텍스트 대체 입력
3. AI 처리 상태와 추가 질문
4. 사건 카드 편집·검토
5. 비밀번호 설정과 사건 확정
6. 발급된 사건번호 표시
7. 사건번호·비밀번호 재조회

기능별로 성공 경로만 구현하지 말고 권한 거부, 녹음 실패, AI 응답 형식 오류, 네트워크 끊김, 인증 실패까지 같은 PR 또는 바로 이어지는 Issue로 처리합니다.

### 단계 6. 2주차 기능을 위험도 순으로 붙이기

1. 해결 가이드 조회, 완료 상태, 순서 변경 및 저장
2. 위치 권한과 기관 목록·지도·경로
3. 경찰서 방문 준비물과 한국어·일본어 스크립트
4. Agora 통역 세션과 원문·번역문·다음 행동 표시
5. 신고서 촬영·파일 선택·presigned 업로드·분석 상태
6. 누락 항목 확인
7. 보험 정보 입력, 근거가 표시된 RAG 결과, 내용 불일치 비교

가이드 순서 변경은 화면에서 먼저 바꾼 뒤 서버 저장 실패 시 원래 순서로 되돌릴지, 재시도 표시를 할지 팀과 정합니다. 업로드는 `presigned URL 발급 → S3 업로드 → 서버에 완료 통지 → 분석 상태 조회` 순서를 API 담당자와 동일하게 구현합니다.

### 단계 7. 통합 QA와 배포 API 전환

최종 주에는 기능 추가보다 아래 시나리오를 반복합니다.

- 일본 여행 중 지갑 분실
- 휴대전화 도난
- 여권 분실
- 위치 권한 거부 후 지역 직접 입력
- 마이크 권한 거부 후 텍스트 입력
- 업로드 중 네트워크 단절 후 재시도
- 앱 종료 후 사건번호로 재조회
- 서버가 AI 형식 오류 또는 외부 API 실패를 반환하는 경우

개발 API에서 HTTPS 배포 API로 바꾼 뒤 Android와 iOS 실기기에서 각각 확인합니다. 웹에서 성공한 것은 모바일 검증 완료로 간주하지 않습니다.

## 5. 매일 작업 루틴

### 작업 시작

1. `develop`의 최신 변경과 내 작업 브랜치 상태 확인
2. 오늘 끝낼 Issue의 완료 조건 확인
3. 필요한 API·스키마·외부 연동 의존성을 팀 채널에 공유
4. 공통 타입 변경이 있으면 코딩 전에 A·B 담당자와 합의

### 구현 중

1. 작은 세로 단위로 구현
2. 정상·로딩·오류·권한 거부 상태를 함께 확인
3. mock과 실제 응답이 같은 Zod 스키마를 사용하는지 확인
4. 실제 개인정보나 비밀값이 로그와 fixture에 없는지 확인
5. 최소 하루 한 번 최신 `develop`과 통합하여 충돌을 일찍 발견

### 작업 종료

```powershell
pnpm --filter mobile typecheck
git diff --check
git status --short
```

그다음 실기기 또는 에뮬레이터에서 변경 흐름을 직접 실행하고 PR에 다음을 남깁니다.

- 변경 이유와 관련 Issue
- 실행 방법
- Android/iOS/Expo Go/development build 중 실제 확인한 환경
- 정상 화면 스크린샷 또는 짧은 영상
- 권한 거부·네트워크 실패 등 확인한 예외 상태
- 아직 mock인 부분과 실제 API 전환 조건
- 다른 담당자가 알아야 할 계약 변경

## 6. 모바일 담당자의 파일 경계

자유롭게 책임지고 변경할 영역:

- `apps/mobile/app/**`
- `apps/mobile/src/**`를 새로 만드는 작업
- 모바일에 필요한 `apps/mobile/app.json`, `package.json`, `tsconfig.json` 변경
- 모바일 실행·테스트 문서

반드시 담당자와 합의하고 변경할 영역:

- `packages/shared/**`: 웹과 서버가 함께 영향받음
- `docs/API.md`: API 담당자와 계약 동기화 필요
- `.env.example`: 인프라와 서버 환경에도 영향 가능
- DB schema와 migration
- 웹 API Route Handler
- Docker, Nginx, AWS, Agora 서버 토큰 코드

모바일에서 필요하다는 이유로 서버 비밀키를 앱에 추가하거나, 공유 스키마를 알리지 않고 바꾸거나, 다른 담당자의 구현을 임시로 대체하지 않습니다.

## 7. 첫 번째 기능 PR 권장 범위

처음 PR은 아래 정도가 적당합니다.

**제목 예시**

```text
feat: 모바일 사건 생성 기본 흐름 구성
```

**포함할 내용**

- Expo Router 기반 홈·새 사건·사건 조회 라우트
- 최소 theme token과 공통 Button/Input/Screen 컴포넌트
- `src/features/case` 구조
- Zod 검증을 통과하는 mock 사건 생성 흐름
- 로딩·검증 오류·중복 제출 방지 상태
- 모바일 실행 방법 보완
- typecheck 통과와 실행 화면

**포함하지 않을 내용**

- 아직 계약이 없는 실제 사건 API를 추측해 구현하는 것
- 마이크·지도·카메라·Agora를 한 PR에서 전부 설치하는 것
- 완성되지 않은 전역 상태 관리나 UI 라이브러리를 대규모로 도입하는 것
- 실제 API 키 또는 개인정보 fixture

## 8. 막혔을 때 질문하는 방법

단순히 “API가 안 돼요”라고 전달하지 말고 다음 정보를 함께 남깁니다.

```text
- 실행 환경: Android 실기기 / Expo Go / Windows
- 호출 주소와 method: POST /api/...
- 기대 상태 코드와 실제 상태 코드
- 요청 시각
- 재현 순서
- 모바일에 표시된 안전한 오류 요약
- 서버 로그 확인이 필요한 request ID
- 제외한 민감정보: 비밀번호, 여권번호, 실제 문서 내용
```

API가 늦어져도 확정 스키마 기반 mock, 화면 상태, 접근성, 권한 거부 흐름은 진행할 수 있습니다. 반대로 API 계약 자체가 정해지지 않은 상태에서 필드와 상태 값을 임의로 확정하지는 않습니다.

## 9. 완료 판단 체크리스트

### 개발 기반

- [ ] 새 팀원이 루트에서 설치 후 모바일 앱을 실행할 수 있다.
- [ ] TypeScript strict 설정에서 typecheck가 통과한다.
- [ ] 실기기에서 API base URL에 접근할 수 있다.
- [ ] route 파일과 기능 로직의 책임이 분리되어 있다.

### 핵심 사용자 흐름

- [ ] 사건 시작부터 사건번호 발급까지 이동할 수 있다.
- [ ] 사건번호와 비밀번호로 다시 조회할 수 있다.
- [ ] 분실과 도난의 조건부 입력이 구분된다.
- [ ] 입력 중복 제출이 방지된다.
- [ ] 뒤로 이동·앱 background 전환 시 입력 정책이 일관된다.

### 오류와 권한

- [ ] 마이크·위치·카메라 권한 거부 대안이 있다.
- [ ] 네트워크 단절과 재시도 가능 여부를 구분해 안내한다.
- [ ] 서버·AI·업로드 오류가 빈 화면이나 무한 로딩으로 남지 않는다.
- [ ] 긴 작업의 진행 상태를 앱 재진입 후 다시 확인할 수 있다.

### 보안과 개인정보

- [ ] 비밀번호 원문과 서버 비밀키를 저장·로그·커밋하지 않는다.
- [ ] 민감정보를 필요한 화면에서 마스킹한다.
- [ ] 실제 개인정보를 개발·시연 fixture로 사용하지 않는다.
- [ ] 사건번호와 비밀번호를 URL이나 분석 이벤트에 함께 기록하지 않는다.

### 협업

- [ ] `packages/shared`와 `docs/API.md`가 실제 계약과 일치한다.
- [ ] PR에 실행 환경, 검증 방법, 화면 자료가 있다.
- [ ] mock인 부분과 후속 Issue가 명시되어 있다.
- [ ] 웹과 같은 API·상태 값으로 통합 확인했다.

## 10. 지금 바로 할 일 요약

처음 자리에 앉았다면 아래 순서대로 진행하면 됩니다.

1. 브랜치와 작업 트리를 확인한다.
2. Corepack으로 프로젝트 지정 pnpm을 준비한다.
3. 의존성을 설치하고 현재 모바일 홈 화면을 실기기 또는 에뮬레이터에서 실행한다.
4. `/api/health`를 기기에서 호출해 모바일↔개발 PC 네트워크를 검증한다.
5. 백엔드·웹 담당자와 화면 목록, 사건 생성·조회 계약, 오류 형식을 먼저 확정한다.
6. 모바일 폴더 구조, 최소 공통 UI, API client 경계를 만든다.
7. 확정된 공통 Zod 스키마를 사용하는 mock으로 사건 생성·재조회 세로 흐름을 완성한다.
8. 실제 사건 API가 준비되면 mock adapter만 실제 adapter로 바꾼다.
9. 마이크·위치·카메라 권한과 거부 대안을 기능별로 추가한다.
10. 가이드 → 지도 → 통역 → 문서 업로드 → 보험 순서로 확장한다.
11. 매일 typecheck와 실기기 통합 테스트를 하고 작은 PR로 공유한다.
12. 마지막 주에는 새 기능보다 전체 시나리오, 오류, 개인정보, 배포 API를 반복 검증한다.

이 순서를 따르면 백엔드와 외부 연동을 무작정 기다리지 않으면서도, 계약이 확정되었을 때 재작업을 최소화할 수 있습니다.

import type { CapturedReportPhoto } from "@/features/report-photo/types/reportPhoto";

export const reportPhotoPreviewFixture: CapturedReportPhoto = {
  uri: "mock://report-photo/session-only",
  mimeType: "image/jpeg",
  fileSize: 120_000,
};
